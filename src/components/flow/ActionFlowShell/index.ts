// ABOUTME: ActionFlowShell barrel export — modal-wrapped flow chrome composing Modal + FlowHeader + body region.
// ABOUTME: Re-exports the component, its prop types, and the FlowStep / FlowVisibleStep unions.

export { ActionFlowShell } from './ActionFlowShell'
export type {
  ActionFlowShellProps,
  FlowStep,
  FlowVisibleStep,
} from './ActionFlowShell'
